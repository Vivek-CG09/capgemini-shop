server:
  port: 8087

spring:
  application:
    name: order-service
  config:
    import: optional:configserver:http://localhost:8090

#  datasource:
#    url: jdbc:postgresql://localhost:5432/CS_Order
#    username: postgres
#    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true