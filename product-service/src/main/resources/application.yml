server:
  port: 8086

spring:
  application:
    name: product-service
  config:
    import: optional:configserver:http://localhost:8090

#  datasource:
#    url: jdbc:postgresql://localhost:5432/CS_Product
#    username: postgres
#    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true